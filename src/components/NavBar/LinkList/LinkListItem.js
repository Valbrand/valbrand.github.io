import React from 'react'
import styled from 'styled-components'

import { spaces } from '../../../styles'

const LinkListItem = ({ label, isActive }) => (
  <a href="#">

  </a>
)

const Root = styled.li`

`

export default LinkListItem
